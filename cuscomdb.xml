<?php
if (isset($_POST['insert']))
{
 $xml = new DomDocument("1.0","UTF-8");
 $xml->load('cuscomdb.xml');

 $cname = $_POST['c_name'];
 $hcomment = $_POST['h_comment'];

 $rootTag = $xml->getElementByTagName("roo")->item(0);

 $infoTag = $xml->createElement("info");
 $nameTag = $xml->createElement("name",$cname);
 $commentTag = $xml->createElement("comment",$hcomment);

    $infoTag->appendChild($nameTag);
    $infoTag->appendChild($commentTag);
    
    $rootTag->appendChild($infoTag);
    $xml->save('cuscomdb.xml');
}
?>
<html>
<body>

<form method = "POST" action = "cuscommentxml.php">
	Customer Comment</br>
	Name <input type = "text" name = "c_name"></br>
	Comment <input type = "text" name = "h_comment"></br>
	<input type = "submit" name "insert" value = "add">
</form>   

</body>
</html>
